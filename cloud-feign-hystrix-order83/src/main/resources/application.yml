server:
  port: 83

eureka:
  client:
    fetch-registry: true
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
spring:
  application:
    name: HystrixOrder83

feign:
  hystrix:
    enabled: true